# Consumption rules

1. Treat the root `PROJECT_TOTAL_PLANNING_IMPLEMENTATION_AND_DELIVERY_INSTRUCTION_v4.5_r2.md` as the current locator/manifest.
2. Reconstruct the authoritative user-supplied payload only by concatenating the registered segments in manifest order with no inserted separator bytes.
3. Verify the full reconstructed SHA-256 before using the payload as exact source evidence.
4. Do not treat a single payload segment as a standalone instruction document.
5. Do not edit an individual payload segment independently; replace source identity, manifest, audit, and affected segments as one approved canon update.
6. The instruction bundle is an execution Thin Adapter and does not replace product Decision/domain owners.
7. The explicit user `기획 완료` gate and Phase B remain required before product BUILD.

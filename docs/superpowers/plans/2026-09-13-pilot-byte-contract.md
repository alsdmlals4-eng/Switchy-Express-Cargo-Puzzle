# Pilot canonical admission / exact restoration

Approved maintenance continuation, no product behavior or Base repin. Actual Windows
Pilot rejected unchanged CRLF scene7506bytes, whose LF7222bytes match the pinned source.
Python admission canonicalizes LF; editor Pilot compares raw against the LF baseline.
Linux transient RESTORE_TARGET_MISMATCH is independent: unchanged rerun passed, not fixed.

Research NOT_MATERIAL: reconcile an existing exact-byte/hash contract, no new engine API
or product design. ADOPT canonical admission already used by Python; retain raw-byte
restoration; REJECT source normalization, vendor changes, baseline repin and retry-as-fix.

1. Preserve actual Windows failure as RED. Add regression guard for separate admission
   and restoration identities. Capture source once before plugin activation.
2. Compare captured LF-normalized text against current pin, retain its actual raw hash
   for all later mutation/restoration checks. Reject disk changes after snapshot.
3. Windows actual Pilot must admit CRLF, then restore its exact raw bytes. Run Python,
   full game regression and Linux required Pilot without weakening any assertions.
4. Five-pass/independent review, CI/normal merge/readback. Add bounded diagnostic snapshots
   only if Linux restore failure recurs; do not invent its cause or hide failures.

Five-pass: only temporary Pilot consumer; finite game unchanged; no assets; pinned vendor
untouched; canonical source admission and exact bytes are separate proof obligations.

# Product-speed stage witness reconciliation

Goal: execute every existing authored Route Book witness at actual ProductScene speed,
retaining all deliberate failure cases. No new maps, rules, solutions or difficulty changes.
Current actual product exported base_speed2.0 and Factory.configure default2.0 disagree
with witness helper defaults4.0. RB08 already explicitly uses2.0.

Research: NOT_MATERIAL for external gameplay comparison: this unit reconciles an
existing approved numerical consumer, not a new design/tuning choice. Actual Scene
property and created TrainController speed are stronger evidence than another game.

Plan (inline, delegated continuation):
1. Load actual ProductScene default in existing witness run; assert it is2.0 and every
   helper-created train speed matches. RED must reveal4.0 sessions before correction.
2. Replace two helper defaults4.0 with2.0; preserve explicit RB08 calls and every
   positive/negative existing driver. Do not loosen time limits or terminal assertions.
3. Run focused witness and full regression; record exact per-stage positive outcomes
   and source-bound numerical receipt if needed for current completion reporting.
4. Independent/five-pass review, owners, exact CI/merge/readback. This establishes
   deterministic authored solutions at product speed, not human timing or optimality.

Five-pass before correction: actual product consumer; no domain expansion; no new
visual material; current fixtures preserve provenance; mismatch must fail rather than
silently scaling timeouts. Native crash and final user review remain separate gates.

## Actual shell completion coverage follow-through

The deterministic factory witnesses cannot prove Main/Product result transitions.
Add a test-only window runner using the existing authored layouts and manual/Auto/
switch commands, actual Product.advance_time at2.0, no injected result or graph mutation.
Run all12 through Main->book->stage->BUILD->RUN->RESULT; assert SUCCESS, empty map/stack,
correct next-stage availability at each book ending. Capture only actual result screens.
Drive accelerated0.05 simulation steps with frame yields; this is not human-paced play.
Negative control disables pickup on RB01 and must fail the same positive assertions.
This catches broken command/terminal wiring which factory-only tests would miss.
No production change is authorized merely because the new test driver is wrong.
Five-pass: actual shell; fixed approved stages; existing art only; fixture provenance;
strict12complete outcomes plus external exit/error checks. Native gate remains separate.
External research NOT_MATERIAL: coverage of existing consumers, no new product design.

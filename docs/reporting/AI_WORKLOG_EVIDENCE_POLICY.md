# AI 활용 작업일지·증빙집 - 프로젝트 운영 기준

2026-09-14 사용자 지시. GitHub의 실제 코드·기획·검증 기록에서 파생되는 월간 보고서다.
기획 정본은 기존 owner를 유지한다. 블루프린트나 협회 지정 정산 양식을 대체하지 않는다.

## 출력과 버전

- 프로젝트: Switchy Express: Cargo Puzzle.
- 지정 위치: C:/Users/user/Documents/증빙서류/9월 증빙서류.
- 파일명: Switchy_Express_Cargo_Puzzle_2026-09_AI활용_작업일지_증빙집_v1.0.pdf.
- 제출 전 보완본도 새 버전과 변경 이유를 남긴다. 이미 발행한 PDF를 조용히 덮어쓰지 않는다.
- 보고서는 해당 발행 시점의 확인 범위를 명시한다. 9월 전체 작업을 누락 없이 확인했다고 추정하지 않는다.
- 프로젝트 원본에는 작성용 manifest, 출처 경로·SHA256, 생성기와 검증 기록을 보관한다.
  개인정보가 있는 결제·계정 원문은 공개 저장소로 복제하지 않는다.

## 날짜와 증거

v1.0 원본 보존 위치:
`C:/Users/user/Documents/증빙서류/9월 증빙서류/Switchy_Express_Cargo_Puzzle_2026-09_AI활용_작업일지_증빙집_v1.0_원본근거`.
발행 당시의 원본 13개를 상대 경로 그대로 보존했고 sidecar의 raw SHA256 전부 일치함을
2026-09-14 재확인했다. domain-green.json이 참조하는 9개 맵/테스트 입력도 별도로
보존하고 해당 receipt의 LF-normalized SHA256으로 확인했다.
이 초판에는 미커밋 로컬 검증 단계의 spec/plan/README가 포함되므로 현재 Git main의
동명 파일과 해시가 달라도 바로 변조나 손실을 의미하지 않는다. 원본 사본과 대조한다.
Git commit만으로 이 중간 바이트를 복원할 수 있다고 주장하지 않는다. 이 보존본은
삭제 대기 대상이 아니며 발행 PDF·sidecar도 변경하지 않는다. 새 발행은 새 버전이다.

작업일/기간, 원본 기록 시각, 사후 기록 작성일, 증빙 캡처일, PDF 발행일을 분리한다.
Git commit 시각은 변경 기록의 시각이지 전체 AI 수행 시간이나 외부 공증이 아니다.
GitHub PR/CI 시각, 원본 세션 시각, 실제 캡처 metadata가 있을 때 각각 출처와 시간대를 쓴다.
파일 수정시각이나 파일명만으로 실제 작업일을 확정하지 않는다.
정확한 작업일이 없으면 '작업일 미확인', 기존 기록을 요약하면 '사후 정리'를 쓴다.

각 항목: 작업 ID, 프로젝트, 작업 전 상태, 이번 변경, 사용 AI의 확인 범위,
입력 원문/스크린샷 유무, 결과물, 실제 소비처, 검증 종류·결과,
미검증 사항, 원본 경로·commit·PR·hash, 기록/캡처/발행 시각.

실제 프롬프트 화면이 없으면 '원본 화면 미수집'으로 남긴다. 채팅처럼 꾸민 이미지나
재구성한 프롬프트를 원본 화면으로 제출하지 않는다. 전사한 요청은 '사용자 요청 전사'다.
현재 도구가 Codex라는 사실과 구독 계정/청구 명의/특정 모델 사용 증명은 별개다.
결제 증빙은 미제공 상태로 두고 금액을 추정·중복 합산하지 않는다.

## 상태와 제출 경계

계획/자산 후보/구현/정적 검사/기계 검사/실제 창/내보낸 PCK/native EXE/
최종 사용자/기기/권리/출시를 구분한다. NOT_RUN과 실패를 PASS로 바꾸지 않는다.
사용자 제공 협약서 해설은 참고자료이며 이 작업에서 원문 메일·협약서·신청서 대조를
수행한 것으로 쓰지 않는다. 협약 체결·지원 기간·크레딧 인정·고지 방식은 원문 및 담당자
확인 대상이다. 보고서 생성은 회신·서명·제출 권한으로 해석하지 않는다.

## 유지비를 줄이는 실행 순서

작업 단위 evidence 저장 -> 승인/진행 owner 갱신 -> 확인 가능한 원본 manifest 선정 ->
날짜순 파생 PDF 생성 -> 모든 페이지 렌더/읽기/내용 검증 -> 지정 폴더 readback.
월말에는 확인한 원본과 누락 항목만 보충한다. 같은 월 구독 영수증은 하나의 증빙 ID로
여러 작업이 참조하도록 하며 비용을 작업 수만큼 곱하지 않는다.
사용 종료한 렌더 중간물은 복구 정보를 남겨 사용자 삭제대기 폴더로만 이동한다.

## 이번 방식의 이유와 한계

현재 상태: 게임 기획/코드/CI/실행 캡처는 있으나 지원사업 전용 입력·결제 증빙은 불완전하다.
개선: 원본을 복제한 또 하나의 기획서 대신 출처/상태 중심 월간 파생 보고서를 만든다.
기대효과: 원본 대조, 정정 이력, 서비스별 찾아보기를 쉽게 하고 날짜 오인·이중 비용 집계를 줄인다.
Base 승격: 프로젝트 적용부터 검증하며 공용 consumer/반복 근거 없는 정책 승격은 하지 않는다.

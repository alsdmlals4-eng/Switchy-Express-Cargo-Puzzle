# GUT regression consumers

This directory is intentionally present in Phase B discovery even before the first dedicated regression script is promoted. Regression tests added later must remain focused GUT consumers and are subject to the same JUnit and exact-head gates.
